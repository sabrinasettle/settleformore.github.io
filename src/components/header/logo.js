import React from "react"
import { Link } from "gatsby"

export default function Logo() {
    return (
        <Link to='/' className="header-logo" id="header-logo" aria-label="Logo and Link for Sabrina Settle's portfolio">SABRINA SETTLE</Link>
    )
}